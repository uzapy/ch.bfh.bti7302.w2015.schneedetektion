<Window x:Class="Schneedetektion.ImagePlayGround.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Schneedetektion.ImagePlayGround"
	   xmlns:xceed="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1000" >
    <Grid>
        <GroupBox x:Name="groupBox0" Header="By date" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" >
            <DatePicker x:Name="datePicker" DisplayDateEnd="2015-06-10" DisplayDateStart="2014-12-02" FirstDayOfWeek="Monday" DisplayDate="2014-12-02" TabIndex="1" SelectedDateChanged="DatePicker_SelectedDateChanged"/>
        </GroupBox>

        <GroupBox x:Name="groupBox1" Header="By time" HorizontalAlignment="Left" Margin="10,62,0,0" VerticalAlignment="Top">
            <StackPanel>
                <xceed:TimePicker x:Name="timePicker" Width="100" TabIndex="2" LostFocus="timePicker_LostFocus" />
            </StackPanel>
        </GroupBox>
        <GroupBox x:Name="groupBox2" Header="By camera" HorizontalAlignment="Left" Margin="10,110,0,0" VerticalAlignment="Top">
            <ListBox x:Name="listBox" HorizontalAlignment="Left" Height="224" Margin="0,0,0,0" Width="100" TabIndex="4" SelectionMode="Multiple" SelectionChanged="listBox_SelectionChanged" />
        </GroupBox>
        <GroupBox x:Name="groupBox3" Header="Size" HorizontalAlignment="Left" Margin="10,362,0,0" VerticalAlignment="Top">
            <Slider x:Name="slider0" HorizontalAlignment="Left" Width="100" VerticalAlignment="Top" TabIndex="5" Maximum="400" Minimum="50" LargeChange="1" Value="50"/>
        </GroupBox>
        <TabControl Margin="128,10,10.333,9.667">
            <TabItem Header="Gallery">
                <ListView x:Name="imageContainer" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Image Source="{Binding UriSource}" Width="{Binding ElementName=slider0, Path=Value}" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </TabItem>
            <TabItem Header="Time-Lapse">
                <StackPanel>
                    <Slider x:Name="slider1" Margin="0,0,0,0" Height="21" VerticalAlignment="Top"/>
                    <Image x:Name="timeLapesImage" Width="{Binding ElementName=slider0, Path=Value}" />
                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
